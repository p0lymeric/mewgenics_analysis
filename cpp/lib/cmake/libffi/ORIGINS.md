The contents of the autoconf directory are generated by libffi's autotools configuration scripts.
They are checked in tree as the configuration routine requires an odd set of tools.

When updating libffi, run regen.bat to regenerate the headers (both MSVC and a Cygwin/MSYS installation with Autotools is required).

libffi is licensed under the MIT License.
